/**
 * @internal
 * Provider Token for the Undici Dispatcher.
 */
export const DISPATCHER = Symbol("Dispatcher");

/**
 * @internal
 * Provider Token for the Http Module Configuration Object.
 */
export const HTTP_MODULE_OPTIONS = Symbol("HttpModuleOptions");
